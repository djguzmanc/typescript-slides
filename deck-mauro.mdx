import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, vsDark } from "@code-surfer/themes";
import Question from './components/Question/Question';
import Answer from './components/Answer/Answer';
import Wildcard from './components/Wildcard/Wildcard';
import WildcardSet from './components/WildcardSet/WildcardSet';
import { optionalError } from './assets/images/ts-intellisense/index';
export const theme = vsDark;

<div style={{ fontSize:'30px', margin:'0 auto', padding: '30px'}}>
  <h1>Bienvenidos a TypeScript 🧙💙🤑</h1>

  <p>Este es un juego diseñado para conocer <a href="https://www.typescriptlang.org/">[TypeScript]</a> en tan solo 50 minutos.</p>
</div>

---

<WildcardSet>
  <Wildcard type="PHONE" used />
  <Wildcard type="PUBLIC" selected />
  <Wildcard type="FIFTY" />
</WildcardSet>

---

<Question question="¿Quién fue el creador de Typescript y a qué empresa pertenece?">
  <Answer>
    Anders Hejlsberg at Microsoft.
  </Answer>
  <Answer>
    Linus Torvalds at Linux Foundation
  </Answer>
  <Answer>
    Brendan Eich at  Mozilla Corporation's
  </Answer>
  <Answer>
    James Arthur Gosling at Sun Microsystems
  </Answer>
</Question>

---

<Question question="¿Es typescript compilado o interpretado?">
  <Answer>
    Interpretado, porque los archivos de .TS no son entendidos por el navegador y se genera codigo JS.
  </Answer>
  <Answer>
    Compilado, los navegadores de hoy en dia tienen la capacidad de interpretar archivos .TS
  </Answer>
  <Answer>
    Interpretado, todo el codigo es transpilados a .JS, pero Tambien existe la posibilidad de ser interpretado por los navegadores actuales.
  </Answer>
  <Answer>
    Compilado, TS es capaz de generar un archive binario. 
  </Answer>
</Question>

---

<Question question="Cuando no conoces el tipo de una variable o de lo que queremos retornar se debe utilizar por defecto: ">
  <Answer>
    Void
  </Answer>
  <Answer>
    Any
  </Answer>
  <Answer>
    Undefined
  </Answer>
  <Answer>
    Null
  </Answer>
</Question>

---

<Question question="Nadie me conoce ni me quiere completamente, Sólo me tengo a mí mism@">
  <Answer>
    Unknown
  </Answer>
  <Answer>
    Any
  </Answer>
  <Answer>
    {"{}"}
  </Answer>
  <Answer>
    Void
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Unknown Example" 
function isString(someVariable: unknown): someVariable is string {
  return true;
}
```

```diff 1[10:18]
```

```diff 1[33:40]
```

```diff 1[43:65]
```

```ts title="Unknown Example" 
function isString(someVariable: unknown): someVariable is string {
  return true;
}

let newVariable: unknown;
if (typeof newVariable === 'string' ) {
  newVariable.toLowerCase(); 
}
```

```diff 7[3:29]
```

</CodeSurfer>

---

<Question question="¿Existe una diferencia entre declarar variables var, let y const en JavaScript respecto a Typescript?">
  <Answer>
    ¿Enserio me preguntas eso?
  </Answer>
  <Answer>
    No lo se rick, parece falso
  </Answer>
  <Answer>
    Seriedad por favor, si existe una diferencia.
  </Answer>
  <Answer>
    Ni Thanos se habria atrevido a tanto.😄
  </Answer>
</Question>

---

<Question question="Si yo quiero colocar un parámetro opcional en una interfaz, función o clase, ¿Cuál es la sintaxis correcta para ello?">
  <Answer>
    ?
  </Answer>
  <Answer>
    ??
  </Answer>
  <Answer>
    ???
  </Answer>
  <Answer>
    |
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}
```

```diff 1[11:18]
```

```diff 4
```

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}

class Marvel implements IHero 🦸{ }
```

```diff 7[7:32]
```

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}

class Marvel implements IHero 🦸{
  constructor(
    public secretName: string, 
    public superPower: string
    ) { }
  
  public getSuperHero(): void {
    console.log(
      `The name is ${this.secretName} and his/her superpower is ${this.superPower}`
      );
  }
}

let wonderWoman = new Marvel('Diana', 'Programmer');
wonderWoman.getSuperHero();
// Result
🦸‍♀️
// The super secret name is Diana and his/her superpower is Programmer
```

```diff 20
``` 

```diff 21
``` 

```diff 24
``` 

</CodeSurfer>

---

<div style={{ fontSize:'30px', margin:'0 auto', padding: '30px'}}>
  <h1>Warning Implementation</h1>

  <img src={optionalError} />
</div>

---

<Question question=" Es una forma sencilla de dar nombres más descriptivos a valores constantes numéricos.">
  <Answer>
    Types
  </Answer>
  <Answer>
    Classes
  </Answer>
  <Answer>
    Enums
  </Answer>
  <Answer>
    Objects
  </Answer>
</Question>

---


<CodeSurfer>

```ts title="Enums Example"
enum PrintMedia📰{
  Newspaper = 'NEWSPAPER',
  Newsletter = 'NEWSLETTER',
  Magazine = 'MAGAZINE',
  Book = 'BOOK'
}
```

``` diff 1
```

```ts title="Enums Example"
enum PrintMedia📰{
  Newspaper = 'NEWSPAPER',
  Newsletter = 'NEWSLETTER',
  Magazine = 'MAGAZINE',
  Book = 'BOOK'
}

function getPrintMediaCode(mediaName: string): string {
  if (mediaName === PrintMedia.Book ) {
      return `You are selected a ${PrintMedia.Book}`
  }
  return PrintMedia.Newspaper;
}
```

```diff 9[21:36]
```

```diff 10[34:53]
```

```diff 11[10:31]
```

</CodeSurfer>

---

<Question question="TypeScript proporciona tres palabras clave para controlar la visibilidad de los miembros de la clase, como propiedades o métodos.">
  <Answer>
    Static, public, private
  </Answer>
  <Answer>
    Public, Void, protected
  </Answer>
  <Answer>
    Static, get, set
  </Answer>
  <Answer>
    Public, protected, private
  </Answer>
</Question>

---


<CodeSurfer>

```ts title="POO Example"
class Hero 🦸{
  constructor(
    public secretName: string,
    private _superPower: string,
    protected isMutant: boolean,
  ) { }

  public getHero(): void {
    console.log(`${this.secretName} and ${this._superPower}`);
  }

  private getSuperPower(): void {
    console.log(`${this._superPower}`);
  }

  protected verifyHero(): string {
    return this.isMutant ? "Es mutante" : "Heroe de sangre pura";
  }

}
```

``` diff 3
``` 

``` diff 4
``` 

``` diff 5
``` 

``` diff 8
``` 

``` diff 12
``` 

``` diff 16
``` 

```ts title="POO Example"
 class Mutant extends Hero {
  constructor(
    secretName: string,
    superPower: string,
    isMutant: boolean
  ) {
    super(secretName, superPower, isMutant);
  }

  getMutant(): void {
    this.getHero();
    console.log(this.verifyHero());
  }
}
```

```diff 7
```

```diff 10
```

```diff 11
```

```diff 12
```

```ts title="POO Example"
let scarletWitch = new Hero('Wanda', 'Tester', false);
let wolverine = new Mutant('Logan', 'Project Manager', true);

scarletWitch.getHero();
wolverine.getMutant();
```

</CodeSurfer>

---

<Question question="¿De que forma se puede realizar casting en TS?">
  <Answer>
    Derived d = new Derived(); Base b = d;
  </Answer>
  <Answer>
    let enteredText = (input as HTMLInputElement).value;
  </Answer>
  <Answer>
    x = int(1)
  </Answer>
  <Answer>
    int myInt = (int) myDouble;
  </Answer>
</Question>

---

<Question question="Es una construcción especial en TypeScript que indica que un valor puede ser de varios tipos.">
  <Answer>
    let value: string | number = "Foo";
  </Answer>
  <Answer>
    let value: string || number = "Foo";
  </Answer>
  <Answer>
    let value: string && number = "Foo";
  </Answer>
  <Answer>
    let value: string are number = "Foo";
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Several Types Example"
class MutantX {
  constructor(
    public secretName: string,
    public superPowers: string | Array<string>,
    protected isMutant: boolean,
  ) { }

  public listOfSuperPowers(): string | Array<string> {
    return this.superPowers;
  }

}

```

```diff 4
```

```diff 8
```


```ts title="Several Types Example"
class MutantX {
  constructor(
    public secretName: string,
    public superPowers: string | Array<string>,
    protected isMutant: boolean,
  ) { }

  public listOfSuperPowers(): string | Array<string> {
    return this.superPowers;
  }

}

let professorX = new MutantX('Kereki', ['Programmer', 'Guru', 'Powerful psychic abilities'], true);
let mystique = new MutantX('Raven', 'Transformation', true);
professorX.listOfSuperPowers(); // ['Programmer', 'Guru', 'Powerful psychic abilities']
mystique.listOfSuperPowers(); // 'Transformation'
```

</CodeSurfer>

---

<Question question="Permiten combinar los miembros de dos o más tipos mediante este operador. Esto le permite combinar tipos existentes para obtener un solo tipo con todas las funciones que necesita.">
  <Answer>
    {"&"}
  </Answer>
  <Answer>
    |
  </Answer>
  <Answer>
    ==
  </Answer>
  <Answer>
    ===
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Combine Example" 
interface Employee {
  work: () => string;
}

interface Manager {
  manage: () => string;
}
```

```ts title="Combine Example" 
interface Employee {
  work: () => string;
}

interface Manager {
  manage: () => string;
}

type Supervisor = Employee & Manager;

// john can both work and manage
let john: Supervisor;
```

```diff 9
```

```diff 12
```

</CodeSurfer>

---

<Question question="Es util para no crean nuevos tipos, podemos nuevos nombres que se refieren a ese tipo y dar la funcionalidad que deseamos">
  <Answer>
    let value: alphanumeric;
  </Answer>
  <Answer>
    let value = alphanumeric;
  </Answer>
  <Answer>
    let value is alphanumeric;
  </Answer>
  <Answer>
    let value as alphanumeric;
  </Answer>
</Question>

---


<CodeSurfer>

```ts title="Type Mapping Example" 
type alphanumeric = string | number;

let documentId: alphanumeric;
documentId = 1232454356;
let passport: alphanumeric = "";
passport = "BC12353412AWR";
```

```diff 1
```

```diff 4
```

```diff 6
```

</CodeSurfer>

---

docs:  
[codesurfer.pomb.us](https://codesurfer.pomb.us)
