import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, vsDark } from "@code-surfer/themes";
import Question from './components/Question/Question';
import Answer from './components/Answer/Answer';
import Wildcard from './components/Wildcard/Wildcard';
import Logo from './components/Logo/Logo';
import Highlight from './components/Highlight/Highlight';
import WildcardSet from './components/WildcardSet/WildcardSet';
import { 
  pluckError,
  pluckSuggestion,
  neverError,
  asConstError,
  guardSuggestion,
  optionalError
} from './assets/images/ts-intellisense/index';

import init from './assets/images/template/init.png';
import end1 from './assets/images/template/end1.png';
import end2 from './assets/images/template/end2.png';
import ahno from './assets/images/ahno.gif';

export const theme = vsDark;

<div style={{ padding: '30px', textAlign: 'center' }}>
  <h1>TypeScript 🧙💙</h1>

  <p>
    ¿Aún no sabes si <a href="https://www.typescriptlang.org/">
    <b>TypeScript</b>
    </a> encaja en tu proyecto?
  </p>

  <p>
    ¿Vale la pena?
  </p>
</div>

---

<img src={init} style={{ height: '100%' }} />

---

<Logo />

---

<Question 
  question="¿Quién fue el creador de Typescript y a qué empresa pertenece?"
  prize="Googlero">
  <Answer>
    Anders Hejlsberg en Microsoft.
  </Answer>
  <Answer>
    Linus Torvalds en Linux Foundation
  </Answer>
  <Answer>
    Brendan Eich en Mozilla Corporation's
  </Answer>
  <Answer>
    James Arthur Gosling en Sun Microsystems
  </Answer>
</Question>

---

<Question 
  question="TypeScript es"
  prize="Googlero voy a tener suerte">
  <Answer>
    Compilado, el lenguaje es traducido a lenguaje de máquina
    que es ejecutado por el navegador.
  </Answer>
  <Answer>
    Interpretado, lo navegadores pueden correr código TypeScript.
  </Answer>
  <Answer>
    Transpilado, porque <i>npm install typescript-interpreter</i>.
  </Answer>
  <Answer>
    Transpilado, TypeScript es traducido a lenguaje JavaScript.
  </Answer>
</Question>

---

<Question 
  question={
    <span>
      Cuando deseas que <i>typescript</i> se comporte como <i>javascript</i>,
      permitiendo que cualquier variable sea cualquier cosa se usa el tipo
    </span>
  }
  prize="Stackoverflow user">
  <Answer>
    void
  </Answer>
  <Answer>
    any
  </Answer>
  <Answer>
    var
  </Answer>
  <Answer>
    unknown
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="any"
// any
let variable: any = 'string';

// string
let variable2 = 'number';

// any
let variable3;
```

```diff 1:2
```

```diff 4:5
```

```diff 7:8
```

```ts title="any"
// any
let variable: any = 'string';

variable.toFloat(); // ☠️
```

</CodeSurfer>

---

<Question 
  question="Nadie me conoce realmente. La gente piensa que me conoce."
  prize="Stackoverflow user con 10 pestañas a la vez">
  <Answer>
    Cobol
  </Answer>
  <Answer>
    Angular
  </Answer>
  <Answer>
    Unknown
  </Answer>
  <Answer>
    PHP
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Unknown Example" 
let newVariable: unknown;
if (typeof newVariable === 'string') {
  newVariable.toLowerCase(); 
}
```

```diff 1[18:24]
```

```diff 2[5:35]
```

```diff 3[15:27]
```

</CodeSurfer>

---

<Question 
  question="Ni Chuck Norris pudo evitar cumplir con este contrato"
  prize="Corrige un bug sin try/catch">
  <Answer>
    <span>
      Interface
    </span>
  </Answer>
  <Answer>
    <span>
      Extends
    </span>
  </Answer>
  <Answer>
    <span style={{ fontSize: '35px' }}>
      Namespace
    </span>
  </Answer>
  <Answer>
    Matrimonio
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Interface Example"
interface IVehicle {
  wheels: number;
  isAutomatic: boolean;
}
```

```ts title="Interface Example"
interface IVehicle {
  wheels: number;
  isAutomatic: boolean;
}

class Car implements IVehicle {
  constructor(
    public wheels: number,
    public isAutomatic: boolean,
  ) {}
}
```

```ts title="Interface Example"
interface IVehicle {
  wheels: number;
  isAutomatic: boolean;
}

class Car implements IVehicle {
  constructor(
    public wheels: number,
    public isAutomatic: boolean,
  ) {}
}

const vehicle: IVehicle = {
  wheels: 4,
  isAutomatic: false,
};
```

</CodeSurfer>

---

<Question 
  question="Si es un parámetro o propiedad opcional, ¿Cuál es la sintaxis correcta para ello?"
  prize="Usa switch en vez de if/else">
  <Answer>
    <Highlight>function</Highlight> foo(param: Optional{'<'}string>) {'{'}}
  </Answer>
  <Answer>
    <Highlight>interface</Highlight> IAnimal {'{'}<br />
      &nbsp;&nbsp;name: string?;<br />
    }
  </Answer>
  <Answer>
    <Highlight>interface</Highlight> IAnimal {'{'}<br />
      &nbsp;&nbsp;name: optional;<br />
    }
  </Answer>
  <Answer>
    <Highlight>interface</Highlight> IAnimal {'{'}<br />
      &nbsp;&nbsp;name?: string;<br />
    }
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}
```

```diff 4
```

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}

class Marvel implements IHero 🦸{ }
```

```diff 7[7:30]
```

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}

class Marvel implements IHero 🦸{
  constructor(
    public secretName: string, 
    public superPower: string,
    ) { }
  
  public getSuperHeroDescription(): string {
    return `${this.secretName} ${this.superPower}`;
  }
}
```

```ts title="Optional Paramater Example"
interface IHero 🦸{
  secretName: string;
  superPower: string;
  superAge?: number;
}

class Marvel implements IHero 🦸{
  constructor(
    public secretName: string, 
    public superPower: string,
    // nope, adding superAge is not required
    ) { }
  
  public getSuperHeroDescription(): string {
    return `${this.secretName} ${this.superPower}`;
  }
}
```

</CodeSurfer>

---

### Warning Implementation
<img src={optionalError} />

---

<Question 
  question="¿De que forma se puede realizar casting en TS?"
  prize="Sabe centrar un div">
  <Answer>
    d = <Highlight>new</Highlight> Vehicle(); <br />
    b: Car = d;
  </Answer>
  <Answer>
    <span style={{ fontSize: '30px' }}>
      <Highlight>let</Highlight> enteredText = (input <Highlight>as</Highlight> <Highlight color="yellow">HTMLInputElement</Highlight>).value;
    </span>
  </Answer>
  <Answer>
    x = number(1)
  </Answer>
  <Answer>
    myNumber = (number) myNumber;
  </Answer>
</Question>

---

<Question 
  question={
  <span>
    Si no existieran librerías como <i>Immutable.js</i>, yo sería tu
    única opción
  </span>
  }
  prize="Trainee">
  <Answer>
    <Highlight>const</Highlight> obj: readonly = {'{'}<br />
      &nbsp;&nbsp;hello: 'world'<br />
    };
  </Answer>
  <Answer>
    <Highlight>const</Highlight> <Highlight>readonly</Highlight> obj = {'{'}<br />
      &nbsp;&nbsp;hello: 'world'<br />
    };
  </Answer>
  <Answer>
    <Highlight>const</Highlight> obj = {'{'}<br />
      &nbsp;&nbsp;hello: 'world'<br />
    } <Highlight>as const</Highlight>;
  </Answer>
  <Answer>
    <Highlight>const</Highlight> obj = {'{'}<br />
      &nbsp;&nbsp;hello: 'world'<br />
    };<br />
    
    <Highlight>Typescript</Highlight>.freeze(obj);
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="as const Example"
const anObjectThatNobodyShouldModify = {
  transactionAmount: 1200, // 🤑
  isApproved: true,
  to: {
    name: 'Mauricio',
    id: '😛'
  }
};
```

```ts title="as const Example"
const anObjectThatNobodyShouldModify = {
  transactionAmount: 1200, // 🤑
  isApproved: true,
  to: {
    name: 'Mauricio',
    id: '😛'
  }
};

// Is allowed to modify the id 
anObjectThatNobodyShouldModify.to.id = '💀';
```

```ts title="as const Example"
const anObjectThatNobodyShouldModify = {
  transactionAmount: 1200, // 🤑
  isApproved: true,
  to: {
    name: 'Mauricio',
    id: '😛'
  }
} as const;

// Typescript complains! 🤩
anObjectThatNobodyShouldModify.to.id = '💀';
```

```ts title="as const Example"
const anObjectThatNobodyShouldModify = {
  transactionAmount: 1200, // 🤑
  isApproved: true,
  to: {
    name: 'Mauricio',
    id: '😛'
  }
} as const;

// Typescript complains! 🤩
anObjectThatNobodyShouldModify.to.id = '💀';

// Typescript 0 - Immutable.js 1 😔
(anObjectThatNobodyShouldModify as any).to.id = '💀';
```

</CodeSurfer>

---

### Error
<img src={asConstError} />

---

<Question 
  question={
    <span>
      Podría marcar la propiedad <i>count</i> como solo lectura y avisarte si
      alguien se atreve a profanarla
    </span>
  }
  prize="Junior">
  <Answer>
    <Highlight>interface</Highlight> MyInterface {'{'}<br />
      &nbsp;&nbsp;<Highlight>niLoPienses</Highlight> count: number;<br />
    }
  </Answer>
  <Answer>
    <Highlight>interface</Highlight> MyInterface {'{'}<br />
      &nbsp;&nbsp;<Highlight>onlyread</Highlight> count: number;<br />
    }
  </Answer>
  <Answer>
    <Highlight>interface</Highlight> MyInterface {'{'}<br />
      &nbsp;&nbsp;<Highlight>F</Highlight> count: number;<br />
    }
  </Answer>
  <Answer>
    <Highlight>interface</Highlight> MyInterface {'{'}<br />
      &nbsp;&nbsp;<Highlight>readonly</Highlight> count: number;<br />
    }
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="readonly Example"
class Car {
  readonly owner: Owner;

  modifyOwner(newOwner: Owner) {
    this.owner = newOwner;
    // TS complains 😉
  }
}
```

```diff 5:6
```

```ts title="readonly Example"
class Car {
  readonly owner: Owner;

  modifyOwner(newOwner: Owner) {
    this.owner = newOwner;
    // TS complains 😉
  }

  updateOwner(holder: Owner) {
    this.owner.name = holder.name;
    // TS allows it 😶
  }
}
```

</CodeSurfer>

---

<Question 
  question={
    <span>
      TypeScript proporciona tres palabras reservadas para controlar 
      la visibilidad de los miembros de la clase, como propiedades o métodos.
    </span>
  }
  prize="Junior Adv">
  <Answer>
    Esto no existe en Javascript
  </Answer>
  <Answer>
    Se puede lograr pero 😴
  </Answer>
  <Answer>
    Es la D al lado
  </Answer>
  <Answer>
    public, protected, private
  </Answer>
</Question>

---


<CodeSurfer>

```ts title="POO Example"
class Hero 🦸{
  constructor(
    public secretName: string,
    private _superPower: string,
    protected isMutant: boolean,
  ) { }

  public getHero(): void {
    console.log(`${this.secretName} and ${this._superPower}`);
  }

  private getSuperPower(): void {
    console.log(`${this._superPower}`);
  }

  protected verifyHero(): string {
    return this.isMutant ? "Es mutante" : "Heroe de sangre pura";
  }

}
```

``` diff 3
``` 

``` diff 4
``` 

``` diff 5
``` 

``` diff 8
``` 

``` diff 12
``` 

``` diff 16
``` 

```ts title="POO Example"
 class Mutant extends Hero {
  constructor(
    secretName: string,
    superPower: string,
    isMutant: boolean
  ) {
    super(secretName, superPower, isMutant);
  }

  getMutant(): void {
    this.getHero();    // public
    this.verifyHero(); // protected
  }
}
```

```diff 10:13
```

</CodeSurfer>

---

<Question 
  question=" Es una forma elegante de dar nombres más descriptivos a valores constantes numéricos."
  prize="Semi Senior">
  <Answer>
    Types
  </Answer>
  <Answer>
    Classes
  </Answer>
  <Answer>
    Enums
  </Answer>
  <Answer>
    Objects
  </Answer>
</Question>

---


<CodeSurfer>

```ts title="Enums Example"
enum PrintMedia📰{
  Newspaper = 'NEWSPAPER',
  Newsletter = 'NEWSLETTER',
  Magazine = 'MAGAZINE',
  Book = 'BOOK'
}
```

```ts title="Enums Example"
enum PrintMedia📰{
  Newspaper,   // 0
  Newsletter,  // 1
  Magazine,    // 2
  Book         // 3
}
```

```ts title="Enums Example"
enum PrintMedia📰{
  Newspaper,   // 0
  Newsletter,  // 1
  Magazine,    // 2
  Book         // 3
}

function favoritePrintMedia(mediaName: PrintMedia): string {
  switch (mediaName) {
    case PrintMedia.Newspaper:
      return `You are a newspaper man! ❤️`
    case PrintMedia.Newsletter:
      return `Newsletter for the win! ❤️`
    case PrintMedia.Magazine:
      return `Magazine for real! ❤️`
    case PrintMedia.Book:
      return `And e-books too! ❤️`
  }
}
```

```diff 10[10:29],12[10:30],14[10:28],16[10:24]
```

</CodeSurfer>

---

<Question 
  question={
    <span>
      Este operador permite combinar los miembros de dos o más tipos,
      obteniendo así un sólo tipo con todas las funciones/propiedades que necesita.
    </span>  
  }
  prize="Semi Senior Adv">
  <Answer>
    <span style={{ fontSize: '35px' }}>
      <Highlight>type</Highlight> NewType = <Highlight>Combine</Highlight>{'<'}TypeA, TypeB>
    </span>
  </Answer>
  <Answer>
    <span style={{ fontSize: '35px' }}>
      <Highlight>type</Highlight> NewType = <Highlight>Merge</Highlight>{'<'}TypeA, TypeB>
    </span>
  </Answer>
  <Answer>
    <span>
      <Highlight>type</Highlight> NewType = TypeA & TypeB
    </span>
  </Answer>
  <Answer>
    <span>
      <Highlight>type</Highlight> NewType = TypeA + TypeB
    </span>
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Combine Example" 
interface Employee {
  work: () => string;
}

interface Manager {
  manage: () => string;
}
```

```ts title="Combine Example" 
interface Employee {
  work: () => string;
}

interface Manager {
  manage: () => string;
}

type Supervisor = Employee & Manager;

// John can both work and manage
let john: Supervisor;
john.work();
john.manage();
```

```diff 9
```

```diff 12:14
```

</CodeSurfer>

---

<Question 
  question="Es una sintaxis especial en TypeScript que indica que un valor puede ser de varios tipos."
  prize="Senior">
  <Answer>
    <Highlight>let</Highlight> value: string | number = "Foo";
  </Answer>
  <Answer>
    <Highlight>let</Highlight> value: string || number = "Foo";
  </Answer>
  <Answer>
    <Highlight>let</Highlight> value: string && number = "Foo";
  </Answer>
  <Answer>
    <Highlight>let</Highlight> value: string <Highlight>and</Highlight> number = "Foo";
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Type Merge"
class MutantX {
  constructor(
    public secretName: string,
    public superPowers: string | Array<string>,
    protected isMutant: boolean,
  ) { }

  public listOfSuperPowers(): string | Array<string> {
    return this.superPowers;
  }

}

```

```diff 4
```

```diff 8[31:52],9[12:27]
```


```ts title="Type Merge"
let professorX = new MutantX('Kereki', [
  'Programmer', 
  'Guru', 
  'Powerful psychic abilities'
], true);

let mystique = new MutantX('Raven', 'Transformation', true);
professorX.listOfSuperPowers(); 
// ['Programmer', 'Guru', 'Powerful psychic abilities']
mystique.listOfSuperPowers(); 
// 'Transformation'
```

```diff 1[18:28],7[16:26]
```

```diff 1[40],2:4,5[1],7[37:52]
```

```diff
```

</CodeSurfer>

---

<Question 
  question={
    <span>¿Qué significa el tipo de variable <i>never</i>?</span>
  }
  prize="Software Architect">
  <Answer>
    Permite que las propiedades de un objeto 
    acepten null o undefined a la vez.
  </Answer>
  <Answer>
    Es un tipo de variable indicando que ésta nunca tendrá valor.
  </Answer>
  <Answer>
    Se usa en generics para que un tipo genérico esté restringido
    a ciertos tipos de variable.
  </Answer>
  <Answer>
    Se usa para que la aplicación&nbsp;<i>never</i>&nbsp;falle.
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="never Example"
type Person = {
  role: 'ADMIN' | 'PLAYER' | 'CLIENT',
}
```

```ts title="never Example"
type Person = {
  role: 'ADMIN' | 'PLAYER' | 'CLIENT',
}

const doSomething = (person: Person) => {
  switch (person.role) {
    case 'ADMIN':
      break;
    case 'PLAYER':
      break;
  }
}
```

```ts title="never Example"
type Person = {
  role: 'ADMIN' | 'PLAYER' | 'CLIENT',
}

const doSomething = (person: Person) => {
  switch (person.role) {
    case 'ADMIN':
      break;
    case 'PLAYER':
      break;
    default:
      // Error here 🥳!
      const a: never = person.role;
  }
}
```

```diff 5:16
```

```ts title="never Example"
type Person = {
  role: 'ADMIN' | 'PLAYER' | 'CLIENT',
}

const doSomething = (person: Person) => {
  switch (person.role) {
    case 'ADMIN':
      break;
    case 'PLAYER':
      break;
    case 'CLIENT':
      break;
    default:
      const a: never = person.role;
  }
}
```

```diff 5:17
```

```ts title="never Example"
const ups = (a: never): never => {
  throw new Error("Didn't expect to reach here 😨");
}

const doSomethingSafer = (person: Person) => {
  switch (person.role) {
    case 'ADMIN':
      break;
    case 'PLAYER':
      break;
    case 'CLIENT':
      break;
    default:
      ups(person.role);
  }
}
```

</CodeSurfer>

---

### Error
<img src={neverError} />

---

<Question 
  question={
    <span>
      Me solían llamar operador diamante, pero soy más que eso, y soy el único que
      nunca te juzgará por generalizar
    </span>
  }
  prize="Architect">
  <Answer>
    <span>Tipo de variable <i>any</i></span>
  </Answer>
  <Answer>
    <span>Tipo de variable <i>unknown</i></span>
  </Answer>
  <Answer>
    <span>Tipo de variable <i>T</i></span>
  </Answer>
  <Answer>
    <span>
      Todos los Web UI son iguales 🙄<br />
      Att: UX Team
    </span>
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Generics Example"
type HTTPResponse = {
  status: number;
  body: any;
  url: string;
  headers: { [key: string]: string };
}
```

```ts title="Generics Example"
type HTTPResponse = {
  status: number;
  body: any;
  url: string;
  headers: { [key: string]: string };
}

const response: HTTPResponse = {
  body: { user: 'Julian', id: 1 },
  headers: { Auth: 'jwt' },
  status: 200,
  url: 'http://api.com/fixTheBug'
};
```

```diff 3[9:11],9[9:33]
```

```ts title="Generics Example"
type HTTPResponse = {
  status: number;
  body: any;
  url: string;
  headers: { [key: string]: string };
}

const response: HTTPResponse = {
  body: { user: 'Julian', id: 1 },
  headers: { Auth: 'jwt' },
  status: 200,
  url: 'http://api.com/fixTheBug'
};

response.body.notAProp; // ☠️
```

```ts title="Generics Example"
type HTTPResponse<T> = {
  status: number;
  body: T;
  url: string;
  headers: { [key: string]: string };
}
```

```ts title="Generics Example"
type HTTPResponse<T> = {
  status: number;
  body: T;
  url: string;
  headers: { [key: string]: string };
}

const genericResponse: GenericHTTPResponse<{
  user: string,
  id: number,
}> = {
  body: { user: 'Julian', id: 1 },
  headers: { Auth: 'jwt' },
  status: 200,
  url: 'http://api.com/fixTheBug'
};
```

```diff 8[44],9:10,11[1],12[9:33]
```

```ts title="Generics Example"
type HTTPResponse<T> = {
  status: number;
  body: T;
  url: string;
  headers: { [key: string]: string };
}

const genericResponse: GenericHTTPResponse<{
  user: string,
  id: number,
}> = {
  body: { user: 'Julian', id: 1 },
  headers: { Auth: 'jwt' },
  status: 200,
  url: 'http://api.com/fixTheBug'
};

// TS complains 💥
genericResponse.body.notAProp;
```

```ts title="Generics Example"
type ConstrainedHTTPResponse<T extends object> = {
  status: number;
  body: T;
  url: string;
  headers: { [key: string]: string };
}
```

```diff 1[32:45]
```

```ts title="Generics Example"
type ConstrainedHTTPResponse<T extends object> = {
  status: number;
  body: T;
  url: string;
  headers: { [key: string]: string };
}

// Type 'string' does not satisfy the constraint 'object'.
const constrainedResponse: ConstrainedHTTPResponse<string> = {
  body: '',
  headers: { Auth: 'jwt' },
  status: 200,
  url: 'http://api.com/fixTheBug'
};
```

```diff 8,9[52:57]
```

```ts title="Generics Example"
type MappedHTTPResponse<T extends object, K = Partial<T>> = {
  status: number;
  body: K;
  url: string;
  headers: { [key: string]: string };
}
```

</CodeSurfer>

---

<Question 
  question={<span>¿Para que sirve el operador <i>keyof</i>?</span>}
  prize="Tech Manager">
  <Answer>
    Retorna true si la llave existe dentro de un objeto
  </Answer>
  <Answer>
    Hacer cast a una variable
  </Answer>
  <Answer>
    Iterar las llaves de un objeto 
  </Answer>
  <Answer>
    Retorna un type que contiene todas las llaves de un objeto
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="keyof Example"
const pluck = (obj, keyName) => {
  return obj[keyName];
}

const pluckedName = pluck({ 
  name: 'Julian', 
  age: 26
}, 'name');

console.log(pluckedName);
// Julian
```

```ts title="keyof Example"
type SmartGuy = { 
  name: string, 
  age: number
};
const pluck = (obj: SmartGuy, keyName: 'name' | 'age') => {
  return obj[keyName];
}

const pluckedName = pluck({ 
  name: 'Julian', 
  age: 26
}, 'name');

console.log(pluckedName);
// Julian
```

```diff 5[31:54]
```

```ts title="keyof Example"
type SmartGuy = { 
  name: string, 
  age: number
};
const pluck = (obj: SmartGuy, keyName: keyof SmartGuy) => {
  return obj[keyName];
}

const pluckedName = pluck({ 
  name: 'Julian', 
  age: 26
}, 'name');

console.log(pluckedName);
// Julian
```

```diff 5[40:54]
```

```diff 12[4:9]
```

</CodeSurfer>

---

### Error
<img src={pluckError} />

### Suggestion
<img src={pluckSuggestion} />

---

<Question 
  question={
    "¿Cuál es la sintaxis para iterar todas las propiedades de otro objeto?"
  }
  prize="Tech Director">
  <Answer>
    <Highlight>type</Highlight> MyType {'{'} <br />
      &nbsp;&nbsp;[P <Highlight>in keyof</Highlight> OtherType]: OtherType[P]<br />
    {'}'}
  </Answer>
  <Answer>
    <Highlight>type</Highlight> MyType {'{'} <br />
      &nbsp;&nbsp;[key <Highlight>in</Highlight> OtherType]: OtherType[key]<br />
    {'}'}
  </Answer>
  <Answer>
    <span style={{ fontSize: '40px' }}>
      <Highlight>type</Highlight> MyType {'{'} <br />
        &nbsp;&nbsp;[<Highlight>foreach key in</Highlight> OtherType]: OtherType[key]<br />
      {'}'}
    </span>
  </Answer>
  <Answer>
    <Highlight>type</Highlight> MyType {'{'} <br />
      &nbsp;&nbsp;[OtherType: key]: OtherType[key]<br />
    {'}'}
  </Answer>
</Question>

---

<WildcardSet>
  <Wildcard type="PHONE" />
  <Wildcard type="PUBLIC" />
  <Wildcard type="FIFTY" />
</WildcardSet>

---

<Question 
  question={
    "¿Cuál es la sintaxis para iterar todas las propiedades de otro objeto?"
  }
  prize="Tech Director">
  <Answer>
    <Highlight>type</Highlight> MyType {'{'} <br />
      &nbsp;&nbsp;[P <Highlight>in keyof</Highlight> OtherType]: OtherType[P]<br />
    {'}'}
  </Answer>
  <Answer>
    <Highlight>type</Highlight> MyType {'{'} <br />
      &nbsp;&nbsp;[key <Highlight>in</Highlight> OtherType]: OtherType[key]<br />
    {'}'}
  </Answer>
  <Answer>
    <span style={{ fontSize: '40px' }}>
      <Highlight>type</Highlight> MyType {'{'} <br />
        &nbsp;&nbsp;[<Highlight>foreach key in</Highlight> OtherType]: OtherType[key]<br />
      {'}'}
    </span>
  </Answer>
  <Answer>
    <Highlight>type</Highlight> MyType {'{'} <br />
      &nbsp;&nbsp;[OtherType: key]: OtherType[key]<br />
    {'}'}
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Mapped Types"
type Warrior = {
  strength: number;
  name: string;
  suitColor: string;
};

/**
 * If your tsconfig file is set to strict null checks,
 * this null assign will throw an error
 */
const warrior: Warrior = {
  name: 'Mauricio',
  strength: null,
  suitColor: 'red',
};
```

```ts title="Mapped Types"
type Warrior = {
  strength: number;
  name: string;
  suitColor: string;
};

// Let's make every property nullable!
// Wolololoooooooooo 🧙
type ConvertedWarrior = {
  [P in keyof Warrior]: Warrior[P] | null;
}

const warrior: Warrior = {
  name: 'Mauricio',
  strength: null,
  suitColor: 'red',
};
```

```ts title="Mapped Types"
type Warrior = {
  strength: number;
  name: string;
  suitColor: string;
};

// Let's make every property nullable!
// Wolololoooooooooo 🧙
type ConvertedWarrior = {
  [P in keyof Warrior]: Warrior[P]?;
}

const warrior: Warrior = {
  name: 'Mauricio',
  strength: null,
  suitColor: 'red',
};
```

```ts title="Mapped Types"
type Warrior = {
  strength: number;
  name: string;
  suitColor: string;
};

// Let's make every property nullable!
// Wolololoooooooooo 🧙
type ConvertedWarrior = {
  [P in keyof Warrior]: Warrior[P] | AnotherType;
}

const warrior: Warrior = {
  name: 'Mauricio',
  strength: null,
  suitColor: 'red',
};
```

```ts title="Mapped Types"
type Warrior = {
  strength: number;
  name: string;
  suitColor: string;
};

// Let's make every property nullable!
// Wolololoooooooooo 🧙
type ConvertedWarrior = {
  [P in keyof Warrior]: ComplexObj<Warrior[P]>;
}

const warrior: Warrior = {
  name: 'Mauricio',
  strength: null,
  suitColor: 'red',
};
```

</CodeSurfer>

---

<Question 
  question={
    <span>
      Si grazna como un pato, camina como un pato y se comporta como un pato, entonces
      es un <i>string</i>
    </span>
  }
  prize="Tech Directors Director">
  <Answer>
    if (obj === 🦆) {'{'}<br />
      &nbsp;&nbsp;// Quack!<br />
    }
  </Answer>
  <Answer>
    if (obj <Highlight>is</Highlight> 🦆) {'{'}<br />
      &nbsp;&nbsp;// Quack!<br />
    }
  </Answer>
  <Answer>
    if (obj <Highlight>instanceof</Highlight> 🦆) {'{'}<br />
      &nbsp;&nbsp;// Quack!<br />
    }
  </Answer>
  <Answer>
    <span style={{ fontSize: '30px' }}>
      <Highlight>function</Highlight> isDuck(obj: unknown): obj <Highlight>is</Highlight> 🦆 {'{'}<br />
        &nbsp;&nbsp;if (obj.quack) <Highlight>return true</Highlight>;<br />
      }<br /><br />
    </span>
  </Answer>
</Question>

---

<WildcardSet>
  <Wildcard type="PHONE" />
  <Wildcard type="PUBLIC" used />
  <Wildcard type="FIFTY" />
</WildcardSet>

---

<Question 
  question={
    <span>
      Si grazna como un pato, camina como un pato y se comporta como un pato, entonces
      es un <i>string</i>
    </span>
  }
  prize="Tech Directors Director">
  <Answer>
    if (obj === 🦆) {'{'}<br />
      &nbsp;&nbsp;// Quack!<br />
    }
  </Answer>
  <Answer>
    if (obj <Highlight>is</Highlight> 🦆) {'{'}<br />
      &nbsp;&nbsp;// Quack!<br />
    }
  </Answer>
  <Answer>
    if (obj <Highlight>instanceof</Highlight> 🦆) {'{'}<br />
      &nbsp;&nbsp;// Quack!<br />
    }
  </Answer>
  <Answer>
    <span style={{ fontSize: '30px' }}>
      <Highlight>function</Highlight> isDuck(obj: unknown): obj <Highlight>is</Highlight> 🦆 {'{'}<br />
        &nbsp;&nbsp;if (obj.quack) <Highlight>return true</Highlight>;<br />
      }<br /><br />
    </span>
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Type guard example"
// ezpz 🍋 squeezy

function someFn(param: string | number) {
  if (typeof param === 'string') {
    param.toLocaleLowerCase();
  } else {
    param.toExponential();
  }
}
```

```diff 4[7:31]
```

```diff 5[11:29]
```

```diff 7[11:25]
```

```ts title="Type guard example"
type Animal = {
  name: string;
  isDomestic: boolean;
}

type Vehicle = {
  wheels: number;
  isAutomatic: boolean;
}
```

```ts title="Type guard example"
type Animal = {
  name: string;
  isDomestic: boolean;
}

type Vehicle = {
  wheels: number;
  isAutomatic: boolean;
}

function pleaseMergeThis(param: unknown) {
  if (isAnimal(param)) {
    console.log(param.isDomestic);
  } else if (isVehicle(param)) {
    console.log(param.isAutomatic);
  }
}
```

```diff 12[7:21],14[14:29]
```

```ts title="Type guard example"
function isAnimal(obj: unknown): obj is Animal {
  if ((obj as object).hasOwnProperty('name')) return true;
  return false;
}

function isVehicle(obj: unknown): obj is Vehicle {
  if ((obj as object).hasOwnProperty('wheels')) return true;
  return false;
}
```

```diff 1[34:46],6[35:48]
```

```diff 1[19:21],6[20:22]
```

```diff
```

```ts title="Type guard example"
function pleaseMergeThis(param: unknown) {
  if (isAnimal(param)) {
    console.log(param.isDomestic);
  } else if (isVehicle(param)) {
    console.log(param.isAutomatic);
  }
}
```

```diff 1[26:39]
```

```diff 3[17:32],5[17:33]
```

```ts title="Type guard example"
// Don't you like the ifs? 😑
```

```ts title="Type guard example"
// Don't you like the ifs? 😑
function onLineGuard(obj: unknown): asserts obj is Vehicle {
  if (!(obj as object).hasOwnProperty('wheels'))
    throw new Error("Not a Vehicle!");
}
```

```ts title="Type guard example"
// Don't you like the ifs? 😑
function onLineGuard(obj: unknown): asserts obj is Vehicle {
  if (!(obj as object).hasOwnProperty('wheels'))
    throw new Error("Not a Vehicle!");
}

function someFn3(param: unknown) {
  onLineGuard(param);
  console.log(param.isAutomatic);
}
```

```diff 2[37:58],4,8
```

</CodeSurfer>

---

### Type guard Suggestion

<img src={guardSuggestion} />

---

<Question 
  question={
    <span>
      No conoces el tipo de un genérico, y yo podría ser
      útil para averiguarlo...
    </span>
  }
  prize="Tech Partner">
  <Answer>
    typeof
  </Answer>
  <Answer>
    instanceof
  </Answer>
  <Answer>
    infer
  </Answer>
  <Answer>
    No es posible
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
const valueWithType: string = data[0];
// Type error: type (string | number | { world: null })[]
// cannot be assigned to type string
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
const valueWithType: string | number | { world: null } = data[0];
```

```diff 2[18:50],3[22:55]
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
const valueWithType: string | number | { world: null } = data[0];

typeof data[0]; 
// Please don't 🤢
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
const valueWithType: string | number | { world: null } = data[0];

typeof data[0]; 
// Please don't 🤢

type FlattenArray<T> = T extends Array<infer R> ? R : T;
```

```diff 8[18:20]
```

```diff 8[24:48]
```

```diff 8[51]
```

```diff 8[55]
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
const valueWithType: string | number | { world: null } = data[0];

typeof data[0]; 
// Please don't 🤢

type FlattenArray<T> = T extends Array<infer R> ? R : T;

const array = ['hello', 3];
const innerValue: FlattenArray<typeof array> = array[0];
// string | number
```

```diff 10[7:11],11[32:43]
```

```ts title="infer Example"
const data = ['hello', 12, { world: null }];
typeof data; // (string | number | { world: null })[]
const valueWithType: string | number | { world: null } = data[0];

typeof data[0]; 
// Please don't 🤢

type FlattenArray<T> = T extends Array<infer R> ? R : T;

const array = ['hello', 3];
const innerValue: FlattenArray<typeof array> = array[0];
// string | number

const data = ['hello', 12, { world: null }];
const value2: FlattenArray<typeof data> = array[0];
// string | number | { world: null }
```

```diff
```

</CodeSurfer>

---

<Question 
  question={
    <span>
      Sin mí no podrías ejecutar algo de lógica antes del método
      original
    </span>
  }
  prize="CTO">
  <Answer>
    <span>Ctrl + C, Ctrl + V</span>
  </Answer>
  <Answer>
    <span>Decorador</span>
  </Answer>
  <Answer>
    <span>Una partida de Warzone</span>
  </Answer>
  <Answer>
    <span>Un <i>if</i></span>
  </Answer>
</Question>

---

<WildcardSet>
  <Wildcard type="PHONE" used />
  <Wildcard type="PUBLIC" used />
  <Wildcard type="FIFTY" />
</WildcardSet>

---

<Question 
  question={
    <span>
      Sin mí no podrías ejecutar algo de lógica antes del método
      original
    </span>
  }
  prize="CTO">
  <Answer removed>
    <span>Ctrl + C, Ctrl + V</span>
  </Answer>
  <Answer>
    <span>Decorador</span>
  </Answer>
  <Answer removed>
    <span>Una partida de Warzone</span>
  </Answer>
  <Answer>
    <span>Un <i>if</i></span>
  </Answer>
</Question>

---

<CodeSurfer>

```ts title="Decorator example"
class FancyComponent {
  deleteUserFromDb(userId: string): void {
    // Delete the user 
  }
}
```

```ts title="Decorator example"
class FancyComponent {
  deleteUserFromDb(userId: string): void {
    // Delete the user 
  }
}

function AreYouSure(prompt: string) {
  return function (
    target: any,
    propertyKey: string,
    descriptor: PropertyDescriptor
  ) {
    let originalMethod = descriptor.value;

    descriptor.value = function (...args: any[]) {
      const confirmation = confirm(prompt);

      if (confirmation) {
        let result = originalMethod.apply(this, args);
        return result;
      }
    }
  }
}
```

```diff 8:23
```

```diff 7
```

```diff 7[21:34],16[36:41]
```

```diff 13
```

```diff 15
```

```diff 16
```

```diff 18[11:22]
```

```diff 19:20
```

```ts title="Decorator example"
class FancyComponent {
  deleteUserFromDb(userId: string): void {
    // Delete the user 
  }
}
```

```ts title="Decorator example"
class FancyComponent {
  @AreYouSure('This action cannot be rolled back')
  deleteUserFromDb(userId: string): void {
    // Delete the user 
  }
}
```

</CodeSurfer>

---

### No me convenciste de usar TypeScript

<img src={ahno} style={{ height: '100%' }} />

#### ¿Ah no?

---

<img src={end1} style={{ height: '100%' }} />

---

<img src={end2} style={{ height: '100%' }} />